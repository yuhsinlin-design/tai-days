node_modules/
out/
dist/
.vite/
build/
bin/
*.tsbuildinfo

# Google service account key — never commit
credentials/

# Derived data mirror — regenerated from content/ by `npm run build:data`.
# public/data/ is the version-controlled source of truth; this is bundled into
# the packaged app via extraResources and does not belong in git.
resources/data/

# Local env
.env

# macOS
.DS_Store

# Design sources & icon exports (regenerated; not needed for build)
branding/

# Local agent/editor files
.claude/
AGENTS.md
